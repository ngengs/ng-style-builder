module.exports = {
  fn: (gulp, callback) => {
    //   TODO: Create jscs test
    //   let runner = [];
    //   const scriptsConfig = configFile(2);
    //   scriptsConfig.test.jscs.forEach((value) => {
    //     let runObject = gulp.src(value.src).
    //       pipe(include().on('error', gutil.log))
    //       .pipe(jscs({configPath: value.config, showStack: true}))
    //       .pipe(jscs.reporter(value.type));
    //
    //     runner.push(runObject);
    //   });
    //
    //   mergeStream(runner);
    callback();
  },
};
